{
  "name": "@sm-bot/bot",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "tsc -p tsconfig.json --watch",
    "lint": "tsc -p tsconfig.json --noEmit",
    "start": "node dist/src/index.js",
    "test": "tsc -p tsconfig.json && node --test dist/src/guild-join.test.js",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@sm-bot/config": "workspace:*",
    "@sm-bot/core": "workspace:*",
    "@sm-bot/db": "workspace:*",
    "discord.js": "^14.27.0"
  },
  "devDependencies": {
    "typescript": "^7.0.2"
  }
}
